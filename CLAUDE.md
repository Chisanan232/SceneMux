# CLAUDE.md

Read [`AGENTS.md`](AGENTS.md). It is the entry point for every agent working in this repository, and
it lists the precedence order of the rules that apply here.

This file exists only so that Claude Code finds that entry point. It is deliberately not a second
copy of the rules: two copies drift, and a drifted rule file is worse than none.
